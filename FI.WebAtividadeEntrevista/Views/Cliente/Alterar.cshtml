@model WebAtividadeEntrevista.Models.ClienteModel
@{
    ViewBag.Title = "Alterar Cliente";
}
@section scripts{
    <script>
        var urlPost = '@Url.Action("Alterar", "Cliente", new { area = "" })';
        var obj = @Html.Raw(Json.Encode(Model));
        var urlRetorno = '@Url.Action("Index", "Cliente", new { area = "" })';
        var urlAlteracaoBeneficiario = '@Url.Action("Alterar", "Beneficiario", new { guid = ViewBag.Guid })';
        var urlDelecaoBeneficiario = '@Url.Action("Alterar", "Beneficiario", new { guid = ViewBag.Guid })';
        var urlBeneficiarioList = '@Url.Action("BeneficiarioList", "Beneficiario")';
        var guid = '@(ViewBag.Guid)';
        var idModalBeneficiario = `modalBeneficiario${guid}`;
    </script>
    @Scripts.Render("~/bundles/altClientes")
    @Scripts.Render("~/bundles/validadorPessoaFisica")
    @Scripts.Render("~/bundles/customizacao")
    @Scripts.Render("~/bundles/mascaraPessoaFisica")
    @Scripts.Render("~/bundles/beneficiarios")

    <script>
        $(document).ready(function () {
            const maskCpf = new CPFMask();
            const maskCep = new CEPMask();
            const maskTelefone = new TelefoneMask();

            maskCpf.init();
            maskCep.init();
            maskTelefone.init();
        });

        function CadastrarBeneficiario() {
            $('#modalBeneficiario' + '@ViewBag.Guid').modal('show');
        }
    </script>
}
<h2>@Html.Raw(ViewBag.Title)</h2>
@Html.Partial("Forms")

@Html.Action("Alterar", "Beneficiario", new { guid = ViewBag.Guid })